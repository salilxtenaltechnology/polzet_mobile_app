<?xml version="1.0" encoding="utf-8"?>
<!-- 
  Place this file in: android/app/src/main/res/xml/network_security_config.xml
  Then add to AndroidManifest.xml in <application> tag:
  android:networkSecurityConfig="@xml/network_security_config"
-->
<network-security-config>
    <!-- 🔒 Default: Only allow HTTPS connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </base-config>
    
    <!-- ✅ Production domains - HTTPS only -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">polzet.com</domain>
        <domain includeSubdomains="true">polzet.in</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
        
        <!-- 🔒 Optional: Add certificate pinning for extra security -->
        <!-- 
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">base64_encoded_hash_here</pin>
            <pin digest="SHA-256">backup_hash_here</pin>
        </pin-set>
        -->
    </domain-config>
    
    <!-- 🔧 DEBUG ONLY: Allow localhost for development -->
    <!-- This will ONLY apply in debug builds, not in release builds -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">10.0.3.2</domain>
    </domain-config>
    
    <!-- 🔓 Debug overrides for development (trusts user certificates) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>